class Story {
  String title;
  String url;
  String hint;
  String imageUrl;
  int id;
  var date;
  bool flag;
  
  Story(this.title, this.url, this.hint, this.imageUrl, this.id, this.date, this.flag);
}